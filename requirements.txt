# Beacon Inspection System Requirements

# Core dependencies (required)
numpy>=1.21.0
opencv-python>=4.5.0
scikit-image>=0.19.0
scikit-learn>=1.0.0
Pillow>=8.0.0

# Testing
pytest>=7.0.0

# Optional: Modbus RTU I/O over Waveshare USB->RS-485 (8CH IO + optional 4CH relay)
# Lazy-imported by inspection_system.app.io.indicator_bus when io.mode == "modbus".
# Install on the Pi only when wiring real hardware.
# pymodbus>=3.6.0

# Optional: Interactive training GUI
# Install with: pip install pygame
pygame>=2.0.0